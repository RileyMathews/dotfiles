#!/usr/bin/env bash
set -euo pipefail

branch_name=${1:-}

if [[ -z "$branch_name" ]]; then
  printf 'usage: wtc <branch-name>\n' >&2
  exit 2
fi

wt switch --no-cd "$branch_name" -x 'tmux-sessionizer {{ worktree_path }}'
