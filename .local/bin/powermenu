#! /bin/sh

chosen=$(printf "Power Off\nCancel" | rofi -dmenu -i -them-str '@import "power.rasi"')

case "$chosen" in
    "Power Off") dunstify "power off" ;;
    "Cancel") dunstify "Canceling" ;;
    *) exit 1 ;;
esac
