This is my dotfiles repository which uses chezmoi to deploy dotfiles.
We have a `./deploy.sh` script that deploys for us but we may sometimes
also need to run chezmoi directly so feel free to do that.
